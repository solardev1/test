# This file contains the application properties for the Spring Boot app

server.port=8080

# springdoc.api-docs.enabled = true
# springdoc.swagger-ui.enabled = true
# springdoc.api-docs.path = /doc
# springdoc.swagger-ui.path = /doc/swagger-ui.html
# springdoc.pathsToMatch = /api/v1/**

spring.datasource.url=jdbc:h2:mem:tasks;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# spring.datasource.name=tasks
spring.datasource.generate-unique-name=false

# This property enables the H2 console for debugging purposes
spring.h2.console.enabled=true

# This property disables the automatic restart of the app when using spring-boot-devtools
# spring.devtools.restart.enabled=false

# Expose health endpoint
management.endpoints.web.exposure.include=health